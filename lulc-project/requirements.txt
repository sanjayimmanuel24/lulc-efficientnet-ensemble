torch>=2.2
torchvision>=0.17
timm>=0.9
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
rasterio>=1.3        # only needed for EuroSATMSDataset (real multispectral GeoTIFFs)
pyyaml>=6.0
pytest>=8.0
matplotlib>=3.8      # for reliability diagrams / result plots, not imported by src/

# Optional, only if you outgrow the plain YAML config in configs/config.yaml
# (see README "Configuration" section for when this is actually worth adding):
# hydra-core>=1.3

# Optional, only if you want managed experiment tracking beyond local logging:
# wandb>=0.16
# mlflow>=2.10
